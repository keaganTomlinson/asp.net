name: asp.net build pipeline 
on: [push]

jobs: 
  build:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v4
    - name: build the docker image 
      run: docker build -t aspdotnet/${{vars.VERSION}} .
      shell: bash 